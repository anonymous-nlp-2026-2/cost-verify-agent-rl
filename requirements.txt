# Core dependencies
torch>=2.0
transformers
accelerate
peft
datasets
vllm==0.8.2
ray>=2.10
hydra-core
wandb

# Environment
gym
gymnasium
gymnasium[toy-text]

# Analysis & utilities
numpy
pandas
matplotlib
tensordict>=0.8.0,<0.9.0
pyarrow>=15.0.0
pylatexenc
torchdata
dill
codetiming
pybind11
IPython

# Optional: flash-attn (requires matching CUDA toolkit)
# pip install flash-attn --no-build-isolation
